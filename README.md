# RandomNumberGenerator
C# console application for a simple random number generator.

Options:
- Select a range of numbers.
- Generate multiple random numbers by iterating through one generation.

What will be improved:
- Print all the executions when using GenerateMultipleNumbers().
- Add checks for valid inputs.
